import { CcardValidatorDirective } from './ccard-validator.directive';

describe('CcardValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CcardValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
